#pragma once
//used with IntroductionToProgramDefinedTypes.cpp
//full defintion of the program defined type we do that when we wanna use it in multiple files 
struct Fraction 
{
	int numerator{};
	int denominator;
};